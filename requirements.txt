# ══════════════════════════════════════════════
# Research Synthesizer – Cloud-Deployable Dependencies
# ══════════════════════════════════════════════

# ── FIX: Streamlit Cloud OOM crash ──
# Force CPU-only PyTorch so it doesn't download 2.5GB of CUDA drivers
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision

# ── Core Agent Framework ──
crewai[tools]
crewai-tools

# ── PDF Parsing ──
PyMuPDF

# ── Vector Database ──
chromadb
pysqlite3-binary  # Fix for Streamlit Cloud SQLite3 version error

# ── ArXiv Search ──
arxiv

# ── Embeddings (local, free) ──
sentence-transformers

# ── NLP / Plagiarism Detection ──
scikit-learn
numpy

# ── Utilities ──
python-dotenv
pydantic
rich

# ── Streamlit UI ──
streamlit
markdown
